#include "Actor.hpp"

// Actor implementation
// Most functionality is template-based and stays in the header,
// but we can add any non-template implementations here if needed in the future.

// Example: If we add virtual methods or complex logic, they would go here.